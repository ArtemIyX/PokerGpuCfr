# Core
numpy>=1.26
scipy>=1.11
numba>=0.59
tqdm>=4.66
pyyaml>=6.0
orjson>=3.10

# Poker / evaluation
pokerkit>=0.5
treys>=0.1.8

# ML / GPU path
torch>=2.3

# CUDA install note:
# If you want GPU support, install the CUDA wheel from PyTorch, e.g.
# pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128

# Data / experiments
pandas>=2.2
matplotlib>=3.8
tensorboard>=2.17
psutil>=6.0
nvidia-ml-py>=12.560.30
rich>=13.7

# Testing / dev
pytest>=8.0
pytest-benchmark>=4.0
ruff>=0.6
mypy>=1.10
